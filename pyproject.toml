[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cuhkx-portfolio"
version = "0.1.0"
description = "Portable constraint decoding and evaluation contracts from a multimodal reasoning project"
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
